# Suggested Commands

- No project-specific local dev, build, lint, format, typecheck, or test commands are documented; implementation and validation occur in the Copilot Studio web UI.
- From the repository root, run `serena memories check` after Serena memory changes to validate memory references.